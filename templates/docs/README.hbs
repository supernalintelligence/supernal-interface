# {{projectName}}

{{#if description}}{{description}}{{else}}A development project{{/if}}

## 🚀 Quick Start

### Prerequisites

- Node.js {{#if nodeVersion}}{{nodeVersion}}{{else}}>=18.0.0{{/if}}
- npm {{#if npmVersion}}{{npmVersion}}{{else}}>=8.0.0{{/if}}

### Installation

```bash
# Clone the repository
git clone {{#if repository}}{{repository}}{{else}}<repository-url>{{/if}}
cd {{kebabCase projectName}}

# Install dependencies
npm install

# Start development server
npm run dev
```

## 📁 Project Structure

```
{{kebabCase projectName}}/
├── src/                    # Source code
{{#if framework}}
{{#ifEquals framework 'react'}}
│   ├── components/         # React components
│   ├── pages/              # Page components  
│   ├── hooks/              # Custom hooks
│   ├── utils/              # Utility functions
│   └── styles/             # Stylesheets
├── public/                 # Static assets
{{/ifEquals}}
{{#ifEquals framework 'vue'}}
│   ├── components/         # Vue components
│   ├── views/              # View components
│   ├── composables/        # Vue composables
│   ├── utils/              # Utility functions
│   └── assets/             # Static assets
{{/ifEquals}}
{{#ifEquals framework 'express'}}
│   ├── routes/             # API routes
│   ├── middleware/         # Express middleware
│   ├── services/           # Business logic
│   └── models/             # Data models
{{/ifEquals}}
{{#ifEquals framework 'cli'}}
│   ├── commands/           # CLI commands
│   ├── utils/              # Utility functions
│   └── types/              # TypeScript types
├── bin/                    # Binary scripts
{{/ifEquals}}
{{else}}
│   ├── components/         # Project components
│   ├── utils/              # Utility functions
│   └── types/              # Type definitions
{{/if}}
├── tests/                  # Test files
├── docs/                   # Documentation
└── package.json           # Project configuration
```

## 🛠️ Development

### Available Scripts

{{#if devCommand}}
- `npm run dev` - {{devCommand}}
{{else}}
- `npm run dev` - Start development server
{{/if}}
{{#if buildCommand}}
- `npm run build` - {{buildCommand}}  
{{else}}
- `npm run build` - Build for production
{{/if}}
- `npm test` - Run tests
{{#ifContains tools 'eslint'}}
- `npm run lint` - Run ESLint
{{/ifContains}}
{{#ifContains tools 'prettier'}}
- `npm run format` - Format code with Prettier
{{/ifContains}}

### Testing

{{#ifContains tools 'jest'}}
This project uses Jest for testing.
{{/ifContains}}
{{#ifContains tools 'vitest'}}
This project uses Vitest for testing.
{{/ifContains}}

```bash
# Run all tests
npm test

# Run tests in watch mode
npm test -- --watch

# Run tests with coverage
npm test -- --coverage
```

## 🚀 Deployment

{{#if deploymentInstructions}}
{{deploymentInstructions}}
{{else}}
### Build

```bash
npm run build
```

### Docker (if enabled)

{{#ifContains tools 'docker'}}
```bash
# Build Docker image
npm run docker:build

# Run Docker container
npm run docker:run
```
{{else}}
Docker is not configured for this project.
{{/ifContains}}
{{/if}}

## 📝 Documentation

{{#if additionalDocs}}
{{additionalDocs}}
{{else}}
Additional documentation can be found in the `docs/` directory.
{{/if}}

## 🤝 Contributing

{{#if contributingGuide}}
{{contributingGuide}}
{{else}}
1. Fork the repository
2. Create a feature branch (`git checkout -b feature/new-feature`)
3. Commit your changes (`git commit -am 'Add new feature'`)
4. Push to the branch (`git push origin feature/new-feature`)
5. Create a Pull Request
{{/if}}

## 📄 License

{{#if license}}
This project is licensed under the {{license}} License.
{{else}}
This project is licensed under the MIT License.
{{/if}}

{{#if author}}
## 👥 Authors

- {{author}}
{{/if}}

{{#if acknowledgments}}
## 🙏 Acknowledgments

{{acknowledgments}}
{{/if}} 